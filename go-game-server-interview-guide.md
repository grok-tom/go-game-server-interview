# Go 游戏服务端面试复习大纲（中级 · 2024–2026）

> 面向：Go 后端 / 游戏服 / 实时多人后端  
> 用法：按「必会 → 加分」推进；每块先能口头讲清，再补细节。  
> 原则：**能说清取舍与踩坑，比背定义更重要。**

---

## 总览：怎么学、优先级

### 面试官通常在听什么
- **原理能否落到工程**：GMP → 你会不会用 pprof 查泄漏；TCP → 你会不会定帧协议 + 心跳。
- **游戏向场景题**：弱网、断线重连、匹配、房间生命周期、状态/帧同步选型。
- **存储与一致性**：Redis 缓存三大问题、分布式锁、MySQL 索引与事务；MQ 削峰。
- **能讲项目故事**：一次延迟尖刺 / 一次 goroutine 泄漏 / 一次匹配超时，怎么定位、怎么改。

### 优先级（建议投入占比）
| 优先级 | 模块 | 占比 | 说明 |
|--------|------|------|------|
| P0 必会 | Go 语言基础（类型/切片/map/interface 等） | 12% | 口述底盘，缺了后面讲不稳 |
| P0 必会 | Go 并发 + GMP/GC 坑 | 22% | 一面硬通货 |
| P0 必会 | 网络（TCP/UDP/KCP/WS + 粘包/心跳/弱网） | 22% | 游戏服差异化 |
| P0 必会 | 游戏服架构（网关/大厅/匹配/房间 + 同步） | 18% | 项目深挖主战场 |
| P1 必会 | Redis / MySQL / MQ | 13% | 几乎必问 |
| P1 必会 | OS 高并发（epoll、进程线程、内存） | 8% | 中级加深度 |
| P2 够用 | 算法与数据结构 | 5% | 中等题练熟即可 |

### 学习方法（工程师向）
1. **口头模板**：概念 → 为什么 → 怎么用 → 坑/边界 → 项目例子（30–90 秒）。
2. **对照项目**：每条知识点挂到你做过的网关、房间、匹配、落库链路。
3. **画图能讲**：GMP 调度、网关分层、状态同步时序、Redis 缓存穿透防护。
4. **不背假数据**：不要编造「某厂通过率」；用公开架构文章 + 自己项目指标说话。

### 标签说明
- **必会**：中级岗位期望能讲清楚  
- **加分**：能展开实现细节或对比选型，拉开差距  

---

## Go 语言基础（面试向）

> 一面常从这里起手。目标：**每条能 30–60 秒讲清「是什么 → 为什么 → 坑」**，再接到并发/网络。

### 必会：类型系统、指针、method 接收者
- **值 / 引用直觉（口语）**：赋值与传参默认拷贝；`slice`/`map`/`chan`/`func`/`interface` 拷贝的是「小头」（header / 描述符），背后数据可共享；`array`/`struct`/`基本类型` 是整值拷贝。
- **指针**：`*T` 存地址；解引用访问同一份数据。需要改调用方可见状态、或避免大结构体拷贝时用指针。
- **struct + method**
  - **值接收**：方法内是副本，改不动原对象；调用方用值或指针都能调（编译器取址/解引用）。
  - **指针接收**：可改原对象；若类型含 `sync.Mutex` 等不可拷贝字段，**必须指针接收**。
  - 面试句：*「接收者选值还是指针，看要不要改状态、拷贝成本、以及类型是否可拷贝；同一类型方法集尽量统一，避免有的值有的指针。」*

#### 口述题
**Q1. Go 是值传递还是引用传递？**  
只有值传递。传 slice/map 时拷贝的是 header，看起来像「引用」，本质仍是拷贝小结构。

**Q2. 值接收和指针接收怎么选？**  
要改接收者或结构体较大 / 含锁 → 指针；小且只读、当值语义 → 值。方法集：`*T` 方法集包含值方法；`T` 不含只定义在指针上的方法。

**Q3. 为什么含 Mutex 的结构体不要值拷贝？**  
锁被拷贝后两份独立，保护失效；应传指针或只嵌入不可复制字段于指针语义类型。

**Q4. `var p *T` 和 `new(T)` 区别一句话？**  
都得到 `*T`；`new` 分配并零值初始化；字面量/`&T{}` 更常见、可带字段初值。

---

### 必会：make vs new
- **`new(T)`**：分配一块零值内存，返回 `*T`。对 slice/map/chan **只得到 nil 头，不能直接用业务语义**（如向 nil map 写会 panic）。
- **`make(T, ...)`**：只用于 **slice / map / chan**，返回**已初始化可用**的 `T`（不是指针）。
- 面试句：*「引用类型要『能用』用 make；只要一个指针指向零值用 new 或 `&`。」*

#### 口述题
**Q1. make 和 new 区别？**  
`new` 通用、返回 `*T` 零值；`make` 专用于 slice/map/chan，返回初始化后的值。

**Q2. `new([]int)` 能 append 吗？**  
得到的是 `*[]int`，指向 nil slice；应对指针解引用再 append，或直接 `make`/`var s []int`。

**Q3. 为什么 map 必须 make（或字面量）才能写？**  
零值 map 是 `nil`，读可以，**写 panic**。

---

### 必会：切片（高频深挖）
- **结构**：`ptr` + `len` + `cap`；底层是数组（或更大数组的一段）。
- **扩容（口述级）**：容量不够时分配新数组并拷贝；大致 **小切片翻倍、较大后按比例增长**（具体策略随版本微调，面试讲「倍增 + 拷贝，旧数组可被 GC」即可，勿背死常量）。
- **截取陷阱**：`s2 := s1[i:j]` 与 `s1` **共享底层数组**；改 `s2` 可能改到 `s1`。需要独立副本用 `copy` / `append([]T(nil), s...)` / `slices.Clone`。
- **传参**：函数收到的是 **header 拷贝**；改元素（下标赋值）对调用方可见，**重绑定 `append` 到新底层**未必写回调用方（除非返回或传 `*[]T`）。
- **`append` 要点**：返回值必须接住；可能触发扩容导致与原 slice 脱钩。

#### 口述题
**Q1. slice 和 array 区别？**  
array 定长值类型；slice 是对底层数组的描述符，可变长，传参拷贝 header。

**Q2. 为什么 sub-slice 改内容会影响原 slice？**  
共享底层数组；要隔离就 `copy`。

**Q3. `append` 一定不改原底层吗？**  
未扩容时可能改同一数组；扩容后写到新数组，原 slice 看不到新元素。

**Q4. `len`/`cap` 含义？传 slice 进函数改 len 能回去吗？**  
len 用了多少、cap 底层还能用到哪。函数内改的是 header 副本的 len，除非返回新 slice 或传指针，否则调用方 len 不变。

**Q5. 从大 slice 截一小段长期持有有什么问题？**  
整块底层数组无法释放 → 内存「泄漏」观感；应 copy 出所需部分。

---

### 必会：map
- **实现直觉**：哈希表（桶 + 溢出桶；扩容/渐进迁移可提一句，不必背源码）。
- **非并发安全**：并发读写可能 fatal；用 mutex / 分片 / `sync.Map`（读多写少）。
- **遍历顺序不定**：每次可能不同；不要依赖顺序。
- **删除**：`delete(m, k)`；对不存在的 key 是空操作。
- **key 要求**：可比较（`==`）；不能是 slice/map/func；struct 作 key 时字段都得可比较。
- **取值**：`v, ok := m[k]`；直接取缺失 key 得零值。

#### 口述题
**Q1. map 并发读写会怎样？怎么修？**  
可能直接 fatal。加锁、分片 map，或读多写少用 `sync.Map`。

**Q2. 为什么 traverse map 顺序不稳定？**  
运行时故意随机化起步，避免依赖顺序的 bug。

**Q3. 哪些类型不能当 key？**  
不可比较类型：slice、map、function 等。

**Q4. `len(map)`、删到空、nil map 读？**  
nil map 可读（得零值）、可 len=0，不可写；delete 对 nil/缺失安全。

---

### 必会：string
- **不可变**：底层可视为只读 byte 序列；改内容要先变 `[]byte` 再转回（产生新 string）。
- **`string` ↔ `[]byte`**：转换通常涉及 **拷贝**（安全默认）；性能热点才考虑更危险的零拷贝手法（面试提「一般别用」即可）。
- **`range` 字符串**：按 **rune（Unicode 码点）** 迭代，下标是 byte offset，不是第几个字符。
- **`len(s)`**：字节数，不是 rune 数；字符数用 `utf8.RuneCountInString` / `range` 计数。

#### 口述题
**Q1. string 为什么改不了中间某个字符？**  
不可变；`s[i]` 是 byte 且只读，需转 `[]byte` 修改后再 `string(...)`。

**Q2. `range s` 和 `for i := 0; i < len(s); i++` 差别？**  
前者按 rune；后者按 byte，中文等会拆坏。

**Q3. 频繁 `string`/`[]byte` 转换有什么代价？**  
拷贝与分配；热点路径减少转换或复用 buffer。

---

### 必会：defer
- **LIFO**：多个 defer 后进先出。
- **参数预计算**：`defer f(x)` 在注册时已算好 `x`；要延迟读变量用闭包 `defer func(){ ... }()`（注意循环变量旧坑，Go 1.22+ 有改善）。
- **与命名返回值**：defer 可改命名返回值；匿名返回值里 defer 改局部变量影响不了已确定的返回副本（经典面试对比）。
- 游戏服场景：解锁、关连接、计时、`recover` 放在 defer。

#### 口述题
**Q1. 多个 defer 执行顺序？**  
后注册的先执行（栈）。

**Q2. 为什么 `defer fmt.Println(time.Since(start))` 时间可能不对？**  
`Since(start)` 在 defer **注册时**就求值了；应 `defer func(){ fmt.Println(time.Since(start)) }()`。

**Q3. defer 能改返回值吗？**  
命名返回值可以在 defer 里赋值修改；非命名返回值通常不能靠改局部变量「写回」。

**Q4. 循环里 defer 的问题？**  
延迟到函数结束才执行，可能堆积或锁很晚才放；循环内用立即执行的函数包一层，锁紧贴临界区。

---

### 必会：interface
- **鸭子类型**：定义方法集即可，隐式实现，无需 `implements`。
- **组成**：类型（动态类型）+ 值（动态值）；断言 / type switch 取具体类型。
- **nil interface 陷阱**：`var p *T = nil`；`var i interface{} = p` 时 **`i != nil`**（类型信息还在）。返回 `error` 时用「真正的 nil」：`return nil`，不要返回「类型非空、值 nil」的接口。
- **类型断言**：`v, ok := i.(T)`；失败且无 ok 会 panic。`switch x := i.(type)` 做分支。

#### 口述题
**Q1. 什么是接口的鸭子类型？**  
只要方法集匹配即实现；包外类型也能满足你的接口。

**Q2. 为什么「返回了 nil 指针却不是 nil error」？**  
接口值含 (type, value)；value 为 nil 但 type 有值时，接口 ≠ nil。

**Q3. 类型断言失败会怎样？**  
单返回值形式 panic；双返回值 `ok=false`。生产代码优先带 ok 或 type switch。

**Q4. 空接口 `any` 和泛型怎么选（一句话）？**  
要编译期类型安全/少断言用泛型；真正异质容器/插件边界才用 `any`。

---

### 必会：error 处理惯例
- **惯例**：返回 `error` 作最后返回值；调用方先处理 err，少用 panic 做业务分支。
- **包装**：`fmt.Errorf("...: %w", err)` 保留因果链。
- **判断**：`errors.Is(err, target)` 比 `==` 更能穿透包装；`errors.As(err, &target)` 取具体类型。
- **哨兵错误** vs **自定义类型**：稳定可比较用 `var ErrXxx = errors.New(...)`；要带上下文用自定义类型 + `As`。
- 面试句：*「业务错误可预期就返回 error；真正无法继续的编程错误才 panic；日志在合适一层打一次，避免每层都包一层刷屏。」*

#### 口述题
**Q1. `%w` 和 `%v` 包错误差别？**  
`%w` 可被 `Is`/`As`/`Unwrap` 链式检查；`%v` 只是格式化文本。

**Q2. 什么时候用 `errors.Is`？**  
判断是否是某哨兵/目标错误（含被包装过）。

**Q3. panic/recover 用在哪？**  
库边界、协程顶层防崩溃；不要当正常控制流。游戏服 worker 里 recover 要打日志并决定是否踢连接。

---

### 必会：泛型（点到为止）
- **何时用**：容器/算法/对多类型重复逻辑，且约束能说清（`comparable`、自定义 interface 约束）。
- **何时不用**：只有一两种具体类型、或接口多态已够用；为了「炫技」上泛型反而损可读性。
- 能说清：*「类型参数在编译期实例化；约束限制可用操作；和 interface 相比少运行时断言。」* 即可，不必默写复杂示例。

#### 口述题
**Q1. 泛型和 interface 怎么选？**  
要静态类型、避免断言、写通用数据结构 → 泛型；跨包插件/异质集合 → interface。

**Q2. 约束 `comparable` 常见用途？**  
map key、相等比较类泛型函数。

---

### 必会：包、可见性、init
- **可见性**：大写开头导出（跨包可用）；小写包内私有。
- **包路径 / 包名**：目录与 import path；避免循环依赖。
- **`init`**：包初始化时自动跑，无参数无返回；多 `init` 有确定顺序但难读。
- **慎用 init**：隐式副作用（注册、读配置、连库）让测试与启动顺序变脆；更推荐显式 `New`/wire、在 `main` 组装。
- 面试句：*「init 适合无可争议的注册表；有 I/O 或可失败逻辑放到显式初始化。」*

#### 口述题
**Q1. 如何控制 API 暴露面？**  
小写内部类型 + 大写工厂/`New`；接口在使用方定义（接受 interface，返回 struct）。

**Q2. init 有什么坏处？**  
副作用隐式、难测、启动失败难处理；循环依赖时更难查。

---

### 加分（拉开差距）
- **逃逸分析一句话**：编译器决定变量在栈还是堆；取地址传出、接口装箱、过大等可能导致逃逸 → 堆分配 ↑、GC 压力 ↑。用 `go build -gcflags="-m"` 看，不要臆测优化。
- **反射何时不该用**：热点路径、可静态表达的逻辑、安全敏感场景；反射慢、易错、破坏可读性。配置反序列化/通用框架边界才常见。
- **`select` + nil channel**：对 **nil channel 的收/发永远阻塞**，利用它在 `select` 里「动态关闭某分支」（把 channel 置 nil 禁用 case），是常见并发技巧。

#### 口述题
**Q1. 什么是逃逸？为何关心？**  
对象拿到堆上；增加分配与 GC。先正确再测，用编译器标志确认。

**Q2. 为什么业务热路径少用 reflect？**  
额外分配与间接调用，p99 差；能 codegen/泛型就别反射。

**Q3. 如何在 select 里临时禁用某个 case？**  
将该 case 的 channel 设为 `nil`，不会误触发；恢复时再赋回有效 channel。

---

## Go 语言与并发（含 GMP、常见坑）

### 必会清单
- **GMP 模型**：G（goroutine）/ M（OS 线程）/ P（逻辑处理器，数量≈`GOMAXPROCS`）
  - 本地队列优先 → 全局队列 → **work stealing**（从其他 P 偷一半）
  - **系统调用阻塞**：M 带着 G 陷入，P 与 M 解绑（hand-off），另找/新建 M 继续跑本地队列
  - **网络 I/O**：走 **netpoller**，G 挂起，P 不空转，可跑别的 G
  - 可说出口：*「P 保证 CPU 不因单个阻塞系统调用而饿死；netpoller 把海量连接挂起成本压到很低。」*
- **Channel vs Mutex**
  - Channel：所有权转移、编排、关闭广播；Mutex：保护共享结构、临界区短
  - 面试句：*「不要为了『Go 风味』硬上 channel；共享 map 用 mutex/分片 map 往往更直接。」*
- **Context**：超时、取消、链路透传；所有出站 RPC/DB/Redis 应可取消
- **常见并发原语**：`sync.WaitGroup`、`Once`、`Pool`、`Map`、`atomic`、`errgroup`
- **Worker Pool / Fan-in Fan-out / 限流**：`golang.org/x/time/rate`（令牌桶）
- **GC 粗线条**：并发三色标记 + **混合写屏障**；STW 短；调优靠少分配、`sync.Pool`、合理 `GOGC`/内存限制，而不是「关掉 GC」

### 加分
- 抢占调度（异步抢占，防死循环饿死）
- 容器里用 `automaxprocs` / 显式设 `GOMAXPROCS`，避免 P 数 ≠ cgroup CPU 配额
- `sync.Pool` 生命周期与 GC 清理；大对象复用策略
- channel 底层：hchan、 sudog、直接发送（sender/receiver 配对）
- interface 的动态类型/值；nil interface 陷阱

### 常见坑（必会能举例）
| 坑 | 现象 | 应对 |
|----|------|------|
| Goroutine 泄漏 | goroutine 数持续涨 | 无缓冲 channel 未配对、无退出条件的 select、`WaitGroup` Add/Done 不对称、网络读无超时 |
| 向已关闭 channel 写 | panic | 约定「只由发送方关闭」；多发送方用独立 done + WaitGroup |
| map 并发写 | fatal | `sync.Mutex` / `sync.Map` / 分片 |
| defer 在循环里拿锁/创建大量 defer | 延迟释放 / 栈深 | 锁紧贴临界区；循环内函数封装 |
| timer/ticker 未 Stop | 泄漏或误触发 | `Stop` + drain（注意 Go 版本差异） |
| 复制含锁结构体 | 锁失效 | 传指针 |

### 可说出口的排查路径
1. `pprof`：`goroutine` / `heap` / `mutex` / `block`  
2. `curl localhost:6060/debug/pprof/goroutine?debug=2` 看重复栈  
3. 压测对比：连接数、P99、分配速率（`allocs/op`）

---

## 计算机网络（游戏服向）

### 必会：协议选型口述
| 协议 | 特点 | 游戏场景 |
|------|------|----------|
| **TCP** | 可靠有序、字节流、队头阻塞、握手/拥塞控制 | 大厅、登录、支付回调、非强实时业务 |
| **UDP** | 无连接、有报文边界、可丢可乱序、延迟低 | 实时位置、语音；需自建可靠/有序层 |
| **KCP** | 基于 UDP 的可靠传输，可配 ARQ；比 TCP 更激进重传 | 弱网移动端对战；注意带宽与重传风暴 |
| **WebSocket** | 基于 TCP 的长连接帧协议，穿透友好 | H5/小游戏、大厅推送；对战仍常另开 UDP/KCP |
| **QUIC（加分）** | UDP + 多路复用，无队头阻塞 | 大厂接入层常见方向 |

**面试句**：*「大厅走 TCP/WS 稳；战斗态优先 UDP/KCP，关键指令可靠通道、位置类可不可靠或降频。」*

### 必会：粘包 / 半包
- **根因**：TCP 是字节流，不保留应用层消息边界（不是「TCP 不可靠」）。
- **方案**：长度前缀（最常用）/ 定长 / 分隔符；接收侧环形缓冲 + 状态机拆包。
- **工程点**：限制最大包长防 OOM；处理半包缓存；恶意超长长度字段直接掐连接。
- UDP **天然有边界**，但要处理丢包、乱序、重复（序号 + 滑动窗口 / KCP）。

### 必会：心跳与会话
- 双向心跳（如 10–30s）+ 连续 N 次超时判死  
- 应用层心跳优于只靠 TCP keepalive（更可控、可带 RTT 采样）  
- Session：连接与逻辑会话解耦；Token/session_key 支持**断线重连**  
- NAT 穿透场景下 UDP 也要心跳保活映射

### 必会：弱网与游戏体验
- 指标：RTT、抖动、丢包率、重传率、乱序率  
- 策略：客户端预测 + 服务端权威校正；关键包可靠、非关键包丢弃过期  
- 降级：差网络降低同步频率/AOI 范围，避免拖垮房间内其他人  
- Nagle / delayed ACK：小包高频场景可 `TCP_NODELAY`

### 加分
- 序列化：Protobuf / FlatBuffers；战斗包尽量小  
- 加密与防重放：序号 + HMAC / 短时 nonce  
- 网关快慢车道：关键指令 vs 尽力而为状态  
- 抓包定位：重传、窗口、RST、半开连接

---

## 操作系统与高并发

### 必会
- **进程 vs 线程 vs goroutine**：地址空间、调度成本；Go 用 M:N 把海量 G 映射到少量 M  
- **用户态 / 内核态、上下文切换**：高并发要少切换、少拷贝  
- **I/O 多路复用**：select/poll vs **epoll**（红黑树管 fd + 就绪链表；LT/ET）  
  - 面试句：*「Go netpoller 在 Linux 上就是 epoll；业务里很少手写 epoll，但要懂为何能撑 C10K/C100K。」*  
- **阻塞 / 非阻塞 / 同步 / 异步**：别混术语；能举「非阻塞 + epoll 通知」例子  
- **内存**：虚拟内存、缺页、RSS vs 堆；游戏服注意大连接缓冲与泄漏  
- **文件描述符上限**：`ulimit -n`、连接泄漏导致 EMFILE

### 加分
- 零拷贝概念：`sendfile` / `splice` / `mmap`（网关转发、静态资源；战斗服收益看场景）  
- 多 Reactor：Accept 与读写分离（理解网关线程模型即可）  
- CPU 亲和、NUMA（压测调优级）  
- 锁与伪共享（cache line）——能提一句就够

### 高并发设计口头要点
- 无锁/少锁、分片、批量、异步化、背压（channel 有界 + 丢弃/降级策略）  
- 限流熔断：令牌桶 / 滑动窗口；过载时保核心路径（登录/匹配/战斗）  

---

## 游戏服务器架构

### 必会：分层与角色
```
客户端 → 接入/网关(Gate) → 大厅/账号/匹配 → 房间/对局(GameServer) → 数据层(Redis/MySQL/MQ)
                ↓
           公共服务：聊天、排行榜、好友、支付、运营配置
```

| 组件 | 职责 | 口试点 |
|------|------|--------|
| **网关** | 连接管理、鉴权、限流、协议转换、路由到逻辑服 | 不跑重业务；扛 IO；可多地域接入 |
| **大厅** | 登录、背包、社交、进匹配前状态 | 可相对有状态或会话粘滞 |
| **匹配** | 池化、分段、超时补人、质量分 | 独立扩缩；结果写入房间创建请求 |
| **房间/对局** | 帧或状态同步、胜负结算、反作弊校验 | 生命周期短；可按需拉起销毁 |
| **数据层** | 缓存热点、持久化、异步日志/战报 | 战斗中少同步刷库 |

### 必会：状态同步 vs 帧同步

| 维度 | 帧同步 Lockstep | 状态同步 State Sync |
|------|-----------------|---------------------|
| 服务端 | 转发/校验输入，客户端共模拟 | **权威**演算，下发状态/差量 |
| 带宽 | 小（输入） | 较大（快照/差量） |
| 反外挂 | 弱，需额外校验 | 强 |
| 断线重连 | 难（追帧） | 易（快照） |
| 确定性 | 要求高（定点数、随机种子） | 要求低 |
| 典型 | MOBA/RTS/部分格斗 | MMO/FPS/大世界/卡牌 |

**面试句**：*「强竞技、逻辑确定性好、带宽紧 → 偏帧同步；大世界、强反作弊、易重连 → 偏状态同步。很多项目是混合：大厅状态同步，战斗帧同步或『逻辑帧 + 差量』。」*

### 必会：房间与匹配细节（易被深挖）
- 房间状态机：创建 → 加载 → 战斗 → 结算 → 销毁；异常：中途退出、挂机、服务器宕机迁移  
- 匹配：段位/延迟分桶、最长等待、AI 补位、取消匹配的并发安全  
- AOI / 兴趣管理：少广播无关实体  
- 时钟：逻辑帧 vs 渲染帧；服务端 `tick`（如 15/20/30 Hz）  

### 加分
- 多地域接入 + 就近 GameServer  
- 战斗服无状态化困难 → 常「房间粘滞 + 宕机重建」  
- 反作弊：关键伤害服务端校验、移动速度校验、回放抽检  
- K8s 弹性：短对局实例冷启动/按需销毁（公开架构文章常见思路）

---

## 存储与中间件（Redis / MySQL / 消息队列）

### Redis（必会）
- **为什么快**：内存、单线程命令执行（避免锁）、I/O 多路复用、高效结构  
- **结构**：String/Hash/List/Set/**ZSet（跳表+字典）**/Bitmap/HyperLogLog；业务对号入座（排行榜 ZSet、会话 Hash）  
- **缓存三问**：穿透（布隆/空值）、击穿（互斥锁或逻辑过期）、雪崩（过期打散 + 高可用）  
- **分布式锁**：`SET key unique NX EX` + **Lua 校验 unique 再删**；看门狗续期；正确性要求高时评估 Redlock/etcd，效率场景单 Redis 常够用  
- **持久化**：RDB / AOF 取舍；主从 + 哨兵 / Cluster 槽位  
- 游戏向：热数据（玩家会话、房间元数据、匹配池）放 Redis；战报异步落库  

### MySQL（必会）
- 索引：B+ 树、覆盖索引、最左前缀、回表；避免隐式转换  
- 事务与隔离级别；MVCC 大意；幻读与间隙锁（能说到 InnoDB 即可）  
- 慢查询：`EXPLAIN`、避免大偏移分页  
- 分库分表思路：按玩家 ID；游戏常「区服自然分片」  
- 读写分离延迟：战斗结算以权威服内存为准，再异步落库  

### 消息队列（必会概念 + 加分落地）
- 用途：削峰、解耦、异步结算/邮件/日志、跨服事件  
- 至少熟悉一种：Kafka / RocketMQ / NATS / NSQ 的**投递语义**（at-least-once → 业务幂等）  
- 顺序：分区键（房间 ID / 玩家 ID）；积压监控与降级  

### 加分
- Redis 热 key / big key 治理  
- 本地缓存（BigCache 等）+ 分布式缓存多层  
- 分布式限流：Redis + Lua  

---

## 算法与数据结构（够用清单）

中级游戏后端：**中等难度 + 能写对边界** 即可，不必刷竞赛。

### 必会题型
- 链表：反转、环检测、合并有序链表  
- 栈队列：括号匹配、单调栈（简化版）  
- 哈希：两数之和、最长无重复子串（滑动窗口）  
- 二叉树：层序、前后中序、最近公共祖先（基础）  
- 排序/查找：快排思想、二分  
- 堆 / TopK：排行榜相关口述 + 手写堆或用容器  
- 图：BFS 最短步数（迷宫类）  
- **跳表**：能讲 Redis ZSet 为何用跳表（加分常问）

### 游戏向加分
- AOI 网格 / 九宫格思路  
- 一致性哈希（扩缩容节点）  
- 限流算法：计数器、滑动窗口、漏桶、令牌桶（能画能比）

### 手写建议（限时 20–30 分钟能写完）
每周保底：滑动窗口 1 题 + 链表/树 1 题 + 栈或堆 1 题。

---

## 高频面试问答示例

> 每题给**口述要点**，面试时控制在 1–2 分钟；追问再展开。

### A. Go 语言与并发（8 题）

**A1. 讲一下 GMP？**  
G 任务、M 线程、P 持有本地队列与运行额度。调度：本地 → 全局 → work stealing。Syscall 时 P hand-off；网络 I/O 靠 netpoller。`GOMAXPROCS` 决定 P 数。

**A2. channel 和 mutex 怎么选？**  
要传递所有权/编排用 channel；保护共享状态用 mutex。复杂扇入扇出可 channel；热点计数器用 atomic。

**A3. 如何发现并处理 goroutine 泄漏？**  
指标上看 goroutine 数；pprof debug=2 找重复栈。根因常见：channel 阻塞、无超时 Read、WaitGroup 不对称。修复：context 取消、超时、退出信号、worker 池限流。

**A4. Go GC 怎么做？怎么减少 STW/压力？**  
三色标记并发 + 混合写屏障；减少堆分配、复用对象、控制 goroutine 与短生命周期小对象风暴；观察 GC 百分位与 CPU。

**A5. 怎样做优雅退出？**  
信号 → cancel context → 停 Accept → 排空 in-flight（有超时上限）→ 关 DB/Redis → 退出。游戏房：先踢人/存档再下线。

**A6. map 是否并发安全？怎么做分片 map？**  
非并发安全。分片：哈希到 N 个带锁小 map，降锁竞争；或 `sync.Map`（读多写少）。

**A7. 实现一个 worker pool 的要点？**  
有界 task channel、固定 worker 数、context 取消、错误汇总（errgroup）、拒绝策略（阻塞/丢弃/降级）。

**A8. 接口 nil 与 nil 指针陷阱？**  
`var p *T = nil` 赋给 `error`/`interface` 后 interface 本身未必 `== nil`（类型信息还在）。返回错误用显式 `return nil`。

### B. 计算机网络（8 题）

**B1. TCP 和 UDP 对比，游戏怎么选？**  
可靠有序 vs 低延迟尽全力。大厅 TCP/WS；实时战斗 UDP/KCP；关键技能可靠，位置可不可靠。

**B2. 什么是粘包？怎么解？**  
字节流无边界。长度头 + 缓冲拆包；限最大长度；半包缓存。UDP 无粘包但有丢乱序。

**B3. 心跳怎么设计？**  
双向周期包 + 超时次数；可测 RTT；与业务包复用或独立命令号；超时进重连流程而非直接清档。

**B4. 断线重连怎么做？**  
Session 与连接分离；重连 Token；服务端保留短时房间视角/快照；帧同步要追帧或切观战，状态同步直接快照。

**B5. KCP 相对 TCP 的优劣？**  
更激进重传、通常延迟更好，弱网友好；可能更费带宽、实现/调参复杂；需防重传放大。

**B6. WebSocket 在游戏里的位置？**  
适合浏览器/大厅长连接；仍是 TCP，有队头阻塞；高强度对战常双通道。

**B7. 如何应对弱网？**  
过期状态丢弃、插值/预测、关键包优先、降 AOI/频率、网关统计抖动后降级；服务端权威防加速器。

**B8. TCP_NODELAY 何时开？**  
小包低频延迟敏感（操作指令）常关 Nagle；大宗吞吐场景再评估。

### C. 操作系统与高并发（6 题）

**C1. epoll 为什么比 select 适合海量连接？**  
内核维护关注集合与就绪列表，避免每次线性扫描与大量 fd 拷贝；复杂度更友好。

**C2. LT 和 ET 区别？**  
LT 有数据就通知；ET 边沿通知，需一次读到 EAGAIN，编程难但Syscall 少。Go 运行时已封装。

**C3. 协程比线程轻在哪？**  
更小栈（可增长）、用户态调度、切换不陷内核；阻塞Syscall 时通过 M/P 模型消化。

**C4. C10K 思路？**  
事件驱动、非阻塞 I/O、少线程多连接、零拷贝/少拷贝、异步日志、连接与业务线程分离。

**C5. 如何排查 FD 耗尽？**  
`lsof`/proc、看是否忘关连接、是否泄漏 timer；调高 ulimit 只是治标。

**C6. 零拷贝了解多少？**  
减少用户/内核拷贝与切换；`sendfile` 等；网关静态或透传有用，战斗逻辑服通常仍要解析协议。

### D. 游戏服务器架构（8 题）

**D1. 网关职责边界？**  
接入、鉴权、限流、路由、协议转换；不塞复杂玩法逻辑，便于水平扩展。

**D2. 状态同步 vs 帧同步怎么选？**  
见表格；强调反作弊、带宽、重连、确定性；可答混合方案。

**D3. 匹配服务如何设计？**  
匹配池按段位/延迟；定时或事件驱动撮合；超时放宽条件；结果创建房间；取消与入池的并发用锁或 Redis 原子结构。

**D4. 房间 Tick 里做什么？**  
收输入 → 模拟/校验 → 广播 → 定时存档点；严格控制单 Tick 耗时，重逻辑异步。

**D5. 如何防止加速/秒杀外挂？**  
服务端权威、技能 CD/伤害公式校验、移动最大速度、异常统计封禁；帧同步加强校验或关键战斗状态同步。

**D6. 百万在线怎么拆？**  
接入与逻辑分离、分区分服或场景分片、无状态服务水平扩、有状态房间单点+迁移、缓存与 DB 分片。

**D7. 战斗服宕机怎么办？**  
短对局可重开；长对局定期快照 + 备援拉起；结算幂等；对玩家补偿策略提前定义。

**D8. 跨服玩法怎么通信？**  
RPC / MQ 事件；注意超时与幂等；匹配跨服时选共同低延迟区。

### E. 存储与中间件（8 题）

**E1. Redis 为什么快？ZSet 底层？**  
内存 + 高效结构 + 多路复用 + 单线程执行命令。ZSet：跳表 + 哈希字典。

**E2. 缓存穿透/击穿/雪崩？**  
穿透：布隆/缓存空值；击穿：热点互斥或逻辑过期；雪崩：TTL 抖动、集群高可用、限流降级。

**E3. Redis 分布式锁正确姿势？**  
`SET NX EX` + 唯一 value；Lua 比对删除；业务可重入另设计；续期用看门狗；持锁代码必须可失败恢复。

**E4. MySQL 索引失效常见原因？**  
函数/运算列、类型转换、前导模糊 `%xx`、不满足最左前缀、OR 不当等。

**E5. 事务隔离级别与默认？**  
读未提交/读已提交/可重复读/串行；InnoDB 默认 RR；用 MVCC + 锁解释脏读、不可重复读、幻读。

**E6. 游戏数据何时写 MySQL？**  
登录拉取、关键道具变更可同步或双写；战斗中批量/异步；下线强制刷。保证幂等与对账。

**E7. MQ 消息重复怎么办？**  
at-least-once 常态；业务幂等键（订单号/结算 ID）；去重表或状态机。

**E8. 热 key 怎么办？**  
本地缓存、拆 key、读写分离/复制、限流；发现靠监控。

### F. 算法口述（5 题）

**F1. 滑动窗口解决什么？**  
最长无重复子串、最小覆盖子串；双指针维护区间合法性。  
**F2. 为什么 Redis 用跳表不手写平衡树？**  
实现简单、区间/范围查询友好、局部性较好。  
**F3. TopK？**  
小顶堆 O(n log k)；或快速选择。  
**F4. 二分边界怎么写不挂？**  
明确搜索空间与不变式；`lo/hi` 开闭约定统一。  
**F5. 检测链表环？**  
快慢指针；入环点再从头同步走。

---

## 7 / 14 天复习节奏建议

### 若只有 7 天（每天 4–6 小时）
| 天 | 重点 | 产出 |
|----|------|------|
| D1 | GMP、channel/mutex、泄漏与 pprof | 口述 A1–A4 + 项目里一次并发 bug |
| D2 | TCP/UDP/KCP/WS、粘包、心跳、弱网 | 画出你项目的协议栈；答 B1–B5 |
| D3 | 网关/匹配/房间 + 状态/帧同步 | 画架构图；准备 1 个同步选型故事 |
| D4 | Redis 全套 + 分布式锁 + 缓存三问 | 答 E1–E3；对照项目 key 设计 |
| D5 | MySQL 索引事务 + MQ 幂等 | 慢查询示例；结算幂等设计 |
| D6 | OS epoll/FD/高并发 + 算法 3 题 | C1–C4；滑动窗口+链表+TopK |
| D7 | 全真模拟：一面八股 45min + 项目深挖 45min | 查漏；整理「不会」清单只补 P0 |

### 若有 14 天（更稳）
| 阶段 | 天 | 内容 |
|------|----|------|
| 夯实 | D1–D3 | Go 并发 + 网络游戏向（同 7 天 D1–D2，多加代码默写拆包/心跳） |
| 架构 | D4–D6 | 游戏架构 + 同步 + 匹配房间状态机；写一页「项目讲解稿」 |
| 存储 | D7–D8 | Redis/MySQL/MQ；锁与缓存；列自己项目的数据流 |
| OS/算法 | D9–D10 | epoll 与高并发；每天 3 道中等算法 |
| 加分 | D11 | KCP/QUIC、反作弊、热 key、优雅退出、压测指标 |
| 模拟 | D12–D13 | 两轮模拟面试；录音看口头废话与卡壳点 |
| 收束 | D14 | 只看错题与项目故事；早睡 |

### 每天固定动作（短而狠）
1. **20 分钟**：3 道算法（计时）  
2. **40 分钟**：一块八股「讲给空气听」  
3. **40 分钟**：项目深挖（延迟、一致性、故障）  
4. **10 分钟**：记 3 条「今天可说出口的句子」  

### 项目深挖自问清单（面试高频）
- QPS/在线/房间人数/P99 延迟大概多少？瓶颈在哪？  
- 一次线上问题：现象 → 指标 → 根因 → 修复 → 预防？  
- 若玩家 ×10，你的网关/匹配/DB 谁先挂？怎么扩？  
- 作弊与恶意识别：协议层做过什么？  

---

## 附：可对外引用的公开资料（选题参考，非「通过率」）

学习时可用于对照架构与考点，**勿伪造公司内推数据**：

1. 《云上游戏服务器架构全解析》— 网关/匹配/对局/同步分层  
   https://blog.csdn.net/qq_33060405/article/details/148775557  
2. 《网易游戏服务端架构揭秘》（掘金）— Gate、帧/状态同步、UDP/QUIC 思路  
   https://juejin.cn/post/7512669428397572111  
3. 《高并发下的存亡博弈：Unity 游戏网关架构》（TrueSight）— 网关职责、KCP/QUIC、弱网降级  
   https://tsight.io/articles/16359709  
4. 《搭建状态同步框架的实践心得》— 状态同步 vs 帧同步对比、KCP+权威服  
   https://jishuzhan.net/article/2063816097983442946  
5. 牛客面经：尚米网络游戏服务器二面讨论 — 分布式分层、帧/状态同步问答结构  
   https://www.nowcoder.com/discuss/860787092626903040  
6. Go 后端面试向：GMP 口述与泄漏（腾讯云社区等）  
   https://cloud.tencent.com/developer/article/2655988  
7. Second Talent: Advanced Golang Backend Interview Questions (2026) — channel/mutex/context/限流  
   https://www.secondtalent.com/interview-guide/golang/  
8. 《游戏服务端编程实践》粘包与心跳 — https://plumephp.com/game-server-in-action-332/  
9. JavaGuide / 网络基础：TCP 粘包与 UDP 场景 — https://javaguide.cn/cs-basics/network/other-network-questions2.html  

---

## 最后提醒

- **先会讲自己的项目**，再补标准答案；面试官更信「我当时 P99 从 x 降到 y」的路径。  
- 游戏服岗位：网络 + 同步 + 房间并发，往往比普通 CRUD 八股更能定级。  
- 保持工程师语气：承认权衡（「我们选状态同步是因为反外挂和重连，带宽用 AOI 压」）。  

祝面试顺利。按 P0 闭环后，再啃加分项。
